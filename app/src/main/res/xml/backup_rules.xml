<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude the entire app sandbox (Room DB, DataStore, SharedPreferences). -->
    <exclude domain="root" path="." />
</full-backup-content>